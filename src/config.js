// App-wide defaults (confirmed in plan Q&A).
export const DEFAULT_SEASON = 2026
export const FIRST_SEASON = 1950
export const TELEMETRY_FIRST_SEASON = 2023 // OpenF1 lower bound

// Reminder lead times (minutes before each session), editable in Settings later.
export const DEFAULT_REMINDER_LEADS = [60, 10]
