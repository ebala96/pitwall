import { PagePlaceholder } from '../components/PagePlaceholder.jsx'

export default function Standings() {
  return <PagePlaceholder title="Standings" note="Drivers + constructors. Milestone 3 (first slice)." />
}
